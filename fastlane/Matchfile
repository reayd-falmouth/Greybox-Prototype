# Fastlane match — S3 storage (same pattern as ScoreCard/GameClock: no git, no MATCH_PASSWORD).
# CI sets MATCH_S3_BUCKET and MATCH_S3_REGION on the ios_testflight job.
storage_mode("s3")
s3_bucket(ENV["MATCH_S3_BUCKET"])
s3_region(ENV["MATCH_S3_REGION"])
s3_skip_encryption(true)
type("appstore")
